import React from "react";

const REPONAME = "REACT";
const OWNERNAME = "FACEBOOK";
const Header = () => {
  return (
    <div>
      {REPONAME}/{OWNERNAME}
    </div>
  );
};

export default Header;
